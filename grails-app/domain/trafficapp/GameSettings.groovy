package trafficapp

class GameSettings {

    int maxIterations
    String algorithm

    // Removed because each user gets random start/end instead of the same start/end for everybody
    //int startNodeID
    //int endNodeID

    static constraints = {
        //add constraints
    }
}
